# Bot 抢答题数据格式与规范

> 适用插件：`koishi-plugin-hidamari-question`（`hidamari-question/` 目录）
> 依据源码：`src/data.ts`（类型定义）、`src/index.ts`（加载/出题/判分逻辑）
> 最后核验：与 `data/answerData/test.json`、`data/answerData-smoke/test.json` 实际数据一致

---

## 1. 题库存放位置

| 路径 | 作用 |
|---|---|
| `hidamari-question/data/answerData/*.json` | 正式本地题库目录（插件配置 `localPath`，默认 `./data/answerData`），每个 `.json` 文件 = 一套题库 |
| `hidamari-question/data/answerData/test.json` | 插件首次使用自动生成的演示题库（内容即 `src/data.ts` 的 `testlocalAnswerLsit`） |
| `hidamari-question/data/answerData-smoke/test.json` | 冒烟测试最小示例 |

加载规则（`src/index.ts` 的 `init()`）：

- 目录下所有 `.json` 后缀文件都会被读取；
- 每个文件按 **`guild` 字段作为唯一 key** 存入内存（`temp[data.guild] = data`），**`guild` 重名会互相覆盖**；
- `JSON.parse` 失败的文件计入失败数并跳过，不影响其它文件；
- 编码统一为 **UTF-8**（不要用 GBK/Shift-JIS）。

## 2. 顶层结构（一个 JSON 文件 = 一套题库）

```json
{
  "msg": "题库描述/题目信息",
  "guild": "题库名称（唯一标识）",
  "pic": "题库封面图 URL，可空字符串",
  "content": { "0": { "...": "单题对象" }, "1": { "...": "单题对象" } }
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `msg` | string | 是 | 题库说明，结算时展示为"详情" |
| `guild` | string | 是 | 题库唯一标识。`/开始抢答 <answerName>` 按此精确匹配选题库；不带参数则随机抽一套 |
| `pic` | string | 是 | 题库封面图 URL，无图填 `""` |
| `content` | object | 是 | 题目集合，key 为字符串形式的题号（`"0"`、`"1"`…），value 为单题对象 |

## 3. 单题对象字段（content 的每一项）

```json
"0": {
  "id": 0,
  "mark": 1,
  "ask": "题目问题文本",
  "more": { "get": ["答对提示"], "lose": ["答错提示"] },
  "susses": ["正确答案1", "正确答案2"],
  "pic": "单题配图 URL（可选）",
  "column": ["选项A", "选项B", "选项C", "选项D"]
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | number | 是 | 题目 id，建议与 content 的 key 一致 |
| `mark` | number | 是 | 本题基础得分。答对 +mark；连续答对第 n 次额外 +n−1（连击加成） |
| `ask` | string | 是 | 问题文本，出题时原样展示 |
| `more` | object | 是 | `get` / `lose` 为字符串数组。⚠️ 当前版本代码未读取该字段，属预留/历史字段，写 `{}` 即可 |
| `susses` | string[] | 是 | 正确答案列表。⚠️ **字段名就是 `susses`**（插件原作者的笔误，必须照写），可写多个，任一匹配即算答对 |
| `pic` | string | 否 | 单题配图 URL，出题时随题发送 |
| `column` | string[] | 是 | 选项列表。**非空 = 选择题；空数组 `[]` = 填空题**（见第 4 节） |

## 4. 关键规范（易踩坑点）

1. **选择题 / 填空题判定**：以 `column` 是否非空判断 —— 非空为选择题，空数组为填空题。
   - ⚠️ 填空题的 `column` 必须显式写成 `[]`，**不能省略该字段**。
     出题格式化 `answerPlayFormat` 中直接执行 `answer.column.map(...)`，字段缺失会直接抛错导致无法出题。
2. **答题交互**：
   - 选择题：玩家发送 `/回答 A`（选项字母）；
   - 填空题：玩家发送 `/回答 正确答案文本`。
3. **判分匹配规则**（`normalizeAnswer` + 结算逻辑）：
   - 比较前将作答和 `susses` 全部去除空白字符（`replace(/\s/g,'')`）后再比对；
   - 选择题作答为字母时，先映射回 `column` 对应下标的文本再比对；
   - 公布答案时会在 `column` 中查找 `susses`，找到则显示为 `A. 答案` 形式，因此**建议 `susses` 文本与 `column` 中某个选项完全一致**（允许空白差异）。
4. **选项数量**：选项按 A、B、C… 展示，代码映射表到 G（第 7 个），实际数据一般为 4~5 个。
5. **每轮题量与随机性**：
   - 一轮抢答只从题库随机抽取 `answersNumOfRush` 道题（配置项，默认 10）；
   - 题目顺序、单题选项顺序都会被随机打乱（`getFreeList`），数据里不用刻意洗牌。
6. **编码**：UTF-8；文件名任意，如 `test.json`、`向阳王.json`。

## 5. 最小完整示例

```json
{
  "msg": "冒烟测试题库",
  "guild": "测试题",
  "pic": "",
  "content": {
    "0": {
      "id": 0,
      "mark": 1,
      "ask": "1+1 等于几？",
      "more": {},
      "susses": ["2"],
      "column": ["2", "3", "4"]
    },
    "1": {
      "id": 1,
      "mark": 1,
      "ask": "太阳从哪边升起？",
      "more": {},
      "susses": ["东边"],
      "column": ["东边", "西边", "北边"]
    }
  }
}
```

## 6. 填空题示例（column 为空数组）

```json
{
  "id": 2,
  "mark": 2,
  "ask": "请填写：向阳素描的作者是____",
  "more": {},
  "susses": ["苍树梅"],
  "column": []
}
```

## 7. 相关配置项（koishi.yml / 插件 Config）

| 配置 | 默认值 | 说明 |
|---|---|---|
| `useLocal` | `false` | 使用本地题库（否则走云端接口） |
| `localPath` | `./data/answerData` | 本地题库存放目录 |
| `answersNumOfRush` | `10` | 每轮抢答抽取的题数 |
| `watingTime` | `60000` | 每轮出题等待时长（毫秒） |
| `watingPlay` | `10000` | 单次回答间隔限制（毫秒） |
| `autoNext` | `10` | 无人作答时自动切题秒数，`0` 表示一直等待 |
| `adminQQ` | `[]` | 管理员 QQ 白名单（可执行 /开始抢答、/结束本题、/结束抢答） |
| `atQQ` | `false` | 回复时是否艾特用户 |
| `useGlobalNick` | `false` | 注册时固定使用平台昵称还是优先群名片 |

## 8. 相关文件索引

- `hidamari-question/src/data.ts` —— 类型定义 + 演示题库
- `hidamari-question/src/index.ts` —— 插件全部逻辑（本规范的最终依据）
- `hidamari-question/lib/data.d.ts` —— 编译后的类型声明
- `hidamari-question/data/answerData/test.json` —— 正式目录中的演示题库
- `hidamari-question/data/answerData-smoke/test.json` —— 冒烟测试题库
- `hidamari-question/scripts/smoke.cjs` —— 端到端冒烟测试脚本

> 注：官方活动网页存档（日文 / 简体 HTML）不是 bot 题库格式，已不放在本仓库。群内用的是 `hidamari-question/data/answerData/*.json`。
